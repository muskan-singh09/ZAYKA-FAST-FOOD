/* =====================================================================
   ADMIN CREDENTIALS  —  RESET PASSWORD FROM HERE
   ---------------------------------------------------------------------
   To change the admin login, simply edit the two values below and save.
   No build step, no server, no database required.
   After editing, open the Admin login page and use the new values.

   NOTE: This is a front-end-only demo. The password lives in plain code,
   so it is fine for a college/portfolio project, but it is NOT real
   security. Do not use a password you use anywhere else.
   ===================================================================== */

const ADMIN_CREDENTIALS = {
  username: "admin",
  password: "muskann123"
};

/* App-wide settings */
const APP_CONFIG = {
  restaurantName: "ZAYKA FAST FOOD",
  currency: "₹"
};
